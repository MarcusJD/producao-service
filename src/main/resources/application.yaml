server:
  port: 8100
spring:
  application:
    name: producao-service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/producao-service?useTimeZone=true&serverTimezone=UTC
    username: root
    password: admin123
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect